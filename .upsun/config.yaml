applications:
  myphpapp:
    type: 'php:8.3' 
    web:
      locations:
        '/':
          # Point the web server strictly to the public folder
          root: 'public'
          passthru: '/index.php'